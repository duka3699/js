for (var sum=0, i = 1; i<=15; i++){ /*начальное значение счетчика; условие; приращение счетчика*/

sum = sum + i;} /*к каждой сумме прибавляется следующее значение*/
{	document.write(sum); /*сумма всех чисел*/
}